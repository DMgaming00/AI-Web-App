{
    "name": "ai-productivity-agent",
    "version": "1.0.0",
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    },
    "engines": {
        "node": "18.x"
    },
    "dependencies": {
        "@google/generative-ai": "^0.24.1",
        "cors": "^2.8.6",
        "dotenv": "^17.3.1",
        "express": "^5.2.1"
    }
}